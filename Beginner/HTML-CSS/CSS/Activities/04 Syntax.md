## Try It Now!

In your external style sheet, add a new CSS rule using what you've just learned about selectors and declaration blocks.

Target your `<h1>` heading using a tag selector, and give it two declarations: a `font-size` and a `color`.

Refresh your browser and check your heading has changed to match your new rule.
