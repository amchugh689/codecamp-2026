Now we are going to some fun function related tasks whoop whoop.

___
### Task 1: Name and Age

Create a function called `DisplayInfo` this function should accept 2 parameters, a persons name and a persons age. It should then display these in the console like so:

"Your name is (name) and you are (age) years old".

___
### Task 2: Adding some Numbers

Create a function called `add2Numbers`, this function should accept 2 parameters called `num1` and `num2` and should return the value of the 2 numbers added together. You can then console.log the result that you get

___

### Task 3: More Circles!!

Create a function called `calculateCircumference` that accepts 1 parameter of the Diameter of a circle and calculates the circumference. The function should then return the result and you can console.log it.