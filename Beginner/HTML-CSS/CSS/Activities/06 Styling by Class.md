## Try It Now!

Add the same class to two or more of your paragraphs, then use the class selector (`.`) in your external style sheet to style all of them at once - try changing their `text-align` and `font-weight`.

Refresh your browser and check that every paragraph with that class has changed, while the others stay the same.
